function display() {
    let x = document.dataform.fname.value;
    let y = document.dataform.lname.value;
    let z = document.dataform.useremail.value;
    alert("FIRST NAME:" + x + "     " + "LAST NAME:" + y + "     " + "EMAIL:" + z);
}